/*
public class Output {
    public static void ResultOut() {
        // String result = Game.CheckForAWord(Input.TakeADictionary(),Input.InputWord());
        Input input = new Input();
        Game result = new Game();
        System.out.printf("Загадано слово: %s Результат: %s", input.RandomWord, result.CheckWord(input.RandomWord, input.InputWord));
    }
}
*/
